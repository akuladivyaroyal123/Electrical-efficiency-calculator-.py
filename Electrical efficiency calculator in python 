# Electrical Efficiency Calculator

input_power = float(input("Enter Input Power (W): "))
output_power = float(input("Enter Output Power (W): "))

efficiency = (output_power / input_power) * 100

print("Electrical Efficiency =", efficiency, "%")